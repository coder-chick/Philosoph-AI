{
  "name": "philosoph-ai-cloud-functions",
  "version": "1.0.0",
  "description": "Cloud Functions for Philosoph-AI document ingestion",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "deploy": "npm run build && gcloud functions deploy ingestPhilosopherTexts --runtime nodejs20 --trigger-http --allow-unauthenticated --region us-central1"
  },
  "dependencies": {
    "@google-cloud/storage": "^7.7.0",
    "@google-cloud/vertexai": "^1.7.0",
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^4.6.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.19",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": "20"
  }
}
